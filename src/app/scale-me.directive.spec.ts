import { ScaleMeDirective } from './scale-me.directive';

describe('ScaleMeDirective', () => {
  it('should create an instance', () => {
    const directive = new ScaleMeDirective();
    expect(directive).toBeTruthy();
  });
});
