import { SatelliteDirective } from './satellite.directive';

describe('SatelliteDirective', () => {
  it('should create an instance', () => {
    const directive = new SatelliteDirective();
    expect(directive).toBeTruthy();
  });
});
